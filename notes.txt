1. friend if already isFriend show

2. login and logout have problem cannot logout immedialy

3. chat and follow router

// updateProfileRoute.js
router.put('/profile', authMiddleware, async (req, res) => {
  try {
    const { fullname, email } = req.body;
    const userId = req.user._id;

    // Update user profile information in the database
    await User.findByIdAndUpdate(userId, { fullname, email });

    res.json({ msg: 'Profile updated successfully' });
  } catch (err) {
    res.status(500).json({ msg: 'Server Error' });
  }
});

// userProfileRoute.js
router.get('/:userId/profile', async (req, res) => {
  try {
    const userId = req.params.userId;
    const user = await User.findById(userId).select('-password'); // Exclude password from the response
    if (!user) return res.status(404).json({ msg: 'User not found' });

    res.json(user);
  } catch (err) {
    res.status(500).json({ msg: 'Server Error' });
  }
});

// Client-side (e.g., in your client application)
setInterval(() => {
    // Send a heartbeat signal to the server
    fetch('/heartbeat', { method: 'POST' });
}, 30000); // Send signal every 30 seconds

// Server-side (e.g., in your Express app)
app.post('/heartbeat', (req, res) => {
    // Update user's online status in the database
    // For example, using Mongoose:
    User.findByIdAndUpdate(req.user._id, { isOnline: true })
        .then(() => {
            res.sendStatus(200);
        })
        .catch(err => {
            console.error('Error updating online status:', err);
            res.sendStatus(500);
        });
});


Posts:
    Create a new post
    Get all posts
    Get a single post by ID
    Update a post
    Delete a post
Comments:
    Create a new comment on a post
    Update a comment
    Delete a comment
Users:
    Register a new user
    Login/logout a user
    Update user profile
    Delete user account
Categories/Tags:
    Get all categories/tags
    Get posts by category/tag
Likes:
    Like/unlike a post
    Like/unlike a comment
Followers/Following:
    Follow/unfollow a user
    Get followers/following list for a user
Search:
    Search posts by keyword
    Search users by username
Miscellaneous:
    Reset password
    Get user activity feed
    Get trending posts

User Routes:
User Registration (POST /api/users/register) done
User Login (POST /api/users/login) done
User Logout (POST /api/users/logout) done
Get User Profile (GET /api/users/profile)
Update User Profile (PUT /api/users/profile)
Delete User Account (DELETE /api/users/profile)

Post Routes:
Create Post (POST /api/posts) done //notification
Get All Posts (GET /api/posts) done
Get Post by ID (GET /api/posts/:id) done
Update Post (PUT /api/posts/:id) done
Delete Post (DELETE /api/posts/:id) done

Comment Routes:
Create Comment (POST /api/comments) done //notification
Update Comment (PUT /api/comments/:id) done
Delete Comment (DELETE /api/comments/:id) done

Like Routes:
Like and Unlike a Post (POST /api/likes/post/:postId) done //notification

Friendship Routes:
Send Friend Request (POST /api/friendships/request) done //notification
Accept/Reject Friend Request (PUT /api/friendships/request/:requestId) done //notification
Get Friends List (GET /api/friendships)
Remove Friend (DELETE /api/friendships/:friendId) done

Chat Routes:
Send Message (POST /api/chats)
Get Chat History (GET /api/chats/:userId)
Delete Chat (DELETE /api/chats/:id)

Notification Routes:
Get Notifications for User (GET /api/notifications)
Mark Notification as Read (PUT /api/notifications/:id)